@{
    string strMessage = "There were validation errors. [" + message + "]";
}

<StatusMessage messageType="error" message="@strMessage" />

@code {
    [Parameter]
    public string message { get; set; }

}
